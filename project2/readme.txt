readme

BinaryClassification.py		Run all the binary classification
MultiClassClassification.py     Run all the multi-class classification
ComputeTF-ICF.py		Find the 10 most significant terms in given classes

ps: The three files are run in Python 2.7.10(Anaconda 2.3.0). Be sure to close the figure when a figure is created to keep the code running.
